[project]
name = "chai"
version = "0.1.0"
description = "Chat with AI in the terminal"
readme = "README.md"
requires-python = ">=3.13.2"
dependencies = [
    "anthropic",
    "google-genai",
    "openai",
    "rich",
]
